mode: append
items:
- Sync the approved Chummer design bundle into `core` under `.codex-design/` and refresh repo-local review context.
- title: Sync the approved Chummer design bundle into `core` under `.codex-design/` and refresh repo-local review context.
  task: Sync the approved Chummer design bundle into `core` under `.codex-design/` and refresh repo-local review context.
  package_id: audit-task-11707
  source_ref: audit_task_candidates[11707]
  audit_finding_key: project.design_mirror_missing_or_stale
  audit_scope_id: core
  allowed_paths:
  - .codex-design
  owned_surfaces:
  - design_mirror:core
source_queue_fingerprint: 39df9410501dedd37328624f545e255700846c52
